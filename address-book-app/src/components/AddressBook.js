import React from 'react';
import SearchBar from './SearchBar';

export default class AddressBook{

    render(){
        return(
            <div>
                <SearchBar/>
            </div>
        )
    }
}